services:
  tech_assignment.route_subscriber:
    # Tell Drupal which class to use
    class: 'Drupal\tech_assignment\Routing\TechAssignmentRouteSubscriber'
    # The below is the event subscriber tag which tells
    # Drupal to trigger this service
    tags:
      - { name: event_subscriber }